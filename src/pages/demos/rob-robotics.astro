---
import DemoLayout from '../../layouts/DemoLayout.astro';
import DemoHeader from '../../components/DemoHeader.astro';
import RobDemo from '../../components/demos/RobDemo';
import LiveAppEmbed from '../../components/demos/LiveAppEmbed';
import LiveAppFallbackRegion from '../../components/demos/LiveAppFallbackRegion.astro';
import { getDemoPageContext } from '../../lib/demo-page';

const { lang, t, demo } = getDemoPageContext(Astro.url, 'rob-robotics');
---

<DemoLayout
  title={demo.title}
  description={demo.metaDescription}
  slug="rob-robotics"
  ogImage="/og/rob-robotics-og.png"
>
  <div class="container">
    <DemoHeader
      badge={demo.badge}
      title={demo.title}
      lead={demo.lead}
      githubUrl={demo.github}
      githubLabel={t('demo.viewSource')}
      accent={demo.accent}
    />

    <LiveAppFallbackRegion>
      <LiveAppEmbed
        slot="embed"
        slug="rob-robotics"
        title="ROB Robotics Dashboard"
        lang={lang}
        client:load
      />

      <div slot="fallback" id="rob-mock-fallback">
        <RobDemo lang={lang} client:visible />
      </div>
    </LiveAppFallbackRegion>
  </div>
</DemoLayout>
